{
  "name": "test-utils",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/test-utils/src",
  "projectType": "library",
  "tags": ["scope:test", "type:test-utils"],
  "// targets": "to see all targets run: nx show project test-utils --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.config.mts packages/test-utils/tests/architecture.test.ts"
      }
    }
  }
}
