# Runtime Adapter Source

This source tree contains runtime strategies, command construction, preflight checks, and process monitoring. It implements application outbound ports and keeps process-specific details at the edge.
