{
  "name": "domain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/domain/src",
  "projectType": "library",
  "tags": ["scope:core", "layer:domain"],
  "// targets": "to see all targets run: nx show project domain --web",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.config.mts packages/domain/tests/run-state.test.ts"
      }
    }
  }
}
